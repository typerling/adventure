node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
*.tsbuildinfo
test-results
playwright-report
blob-report
playwright/.cache

*storybook.log
storybook-static

# Local tooling state — background subagents (e.g. issue implementers spawned from planning
# sessions) work in isolated git worktrees under .claude/worktrees/, not repo content.
.claude
